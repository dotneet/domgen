{
  "name": "domgen",
  "version": "1.0.2",
  "main": "domgen.js",
  "files": [
    "domgen.js"
  ],
  "description": "domgen is tiny and intelligent DOM generating helper.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dotneet/domgen.git"
  },
  "bugs": "https://github.com/dotneet/domgen/issues",
  "devDependencies": {
    "uglifyjs": "^2.4.10"
  },
  "scripts": {
    "build": "uglifyjs domgen.js -c -m > domgen.min.js",
    "archive": "git archive HEAD --prefix=domgen/ --format=tar.gz --output=domgen.tar.gz"
  }
}
